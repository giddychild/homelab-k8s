# ingress-nginx Helm values.
controller:
  # 2 replicas for ingress HA (we have 3 workers).
  replicaCount: 2
  service:
    type: LoadBalancer
    annotations:
      # Pin the ingress to a stable LAN IP from the Cilium pool, so DNS can point at it.
      io.cilium/lb-ipam-ips: "192.168.216.230"
