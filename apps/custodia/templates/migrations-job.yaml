# Migrations run as an initContainer on the api Deployment (see api.yaml), which
# starts only once CloudNativePG has created the DB secret + service and waits/
# retries until the database is ready. A PreSync Job would run before the CNPG
# cluster exists, so it was removed. This file is intentionally empty.
